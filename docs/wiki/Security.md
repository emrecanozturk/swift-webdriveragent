# Security

SwiftWDA has no built-in HTTP authentication. Run it only on trusted devices and route it only through trusted hosts or authenticated gateways.

Do not commit signing files, certificates, profiles, private bundle ids, device identifiers, or internal host paths.

Report vulnerabilities through the repository security policy.
