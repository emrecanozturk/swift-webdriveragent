# Operations

Monitor these signals during rollout:

- Time to `ServerURLHere`.
- `/status` readiness.
- `/metrics` request and error counts.
- Source and screenshot latency.
- Session continuity after MJPEG access.
- Native location support by iOS version.

Run runtime smoke tests only after the runner is reachable from the host.
